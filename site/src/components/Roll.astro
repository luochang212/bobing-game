---
import Die from './Die.astro';
interface Props { faces: number[] }
const { faces } = Astro.props;
---
<span class="roll" role="img" aria-label={`骰面示例：${faces.join('、')}`}>
  {faces.map(value => <Die value={value} />)}
</span>
